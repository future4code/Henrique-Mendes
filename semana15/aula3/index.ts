

// --------------------- Exercicio 1 ---------------------
// Todas as propriedades foram impressas porque uma interface tem suas propriedas sempre como publicas

// --------------------- Exercicio 2 ---------------------
// a. Cannot create an instance of an abstract class
// b. Para se criar uma instancia dessa class, precisamos criar uma classe filha dela para poder instancia-la

// --------------------- Exercicio 3 ---------------------
// a. Remover o atributo abstract
// b. Place nao eh uma interface porque ela tem uma propriedade com protected
// c. Place eh uma classe abstrata porque primeiro nao precisamos instanciar nada com apenas essa classe e segundo poruqe ela eh mais util para as outras classes

// --------------------- Exercicio 4 ---------------------
// Implementado nos codigos

// --------------------- Exercicio 5 ---------------------
// a. As semelhancas sao que ambas utilizam a mesma interface e apesar de serem subclasses de classes diferentes, suas classes maes possuem a mesma classe mae, sendo quase a mesma classe para o CommercialClients e ResidencialClients
// b. As diferencas estao apenas no metodo de calculo da conta e a propriedade peculiar de cada uma (cpf e cnpj)

// --------------------- Exercicio 6 ---------------------
// a. Deve ser filha da Industry porque ela compartilha das mesmas propriedades dessa classe mas tem apenas um atributo extra, por isso nao utilizar a classe Industry e criar uma nova
// b. Deve utilizar a interface Clients, uma vez que todos os atributos desssa interface sao precisos dentro dessa nova classe
// c. os set podem ser utilizados atras da classe mae Industry e o unico atributo diferente dessa classe nao precisa sofrer alteracoes, sendo um registro fixo

// --------------------- Exercicio 7 ---------------------
//